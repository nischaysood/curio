# ---------------------------------------------------------------------------
# PINNED ON PURPOSE. Do not let the IDE talk you into upgrading before Sept 30.
#
# AGP 9.0 removed support for applying `com.android.application` to a Kotlin
# Multiplatform module — it requires migrating to the Android-KMP plugin. That
# migration is a day of work and it buys this project nothing before the deadline.
# Android Studio 2026.x will offer the upgrade on every sync. Say no.
#
# This trio is mutually compatible and known-good:
#   AGP 8.7.3  +  Kotlin 2.1.0  +  Compose Multiplatform 1.7.3  +  Gradle 8.11.1
#
# Revisit after Sept 30, not before.
# ---------------------------------------------------------------------------

[versions]
agp = "8.7.3"
# Play requires targetSdk 36 as of 2026 and rejects uploads below it. AGP 8.7.3
# predates 36, so it warns that the combination is untested — suppressed in
# gradle.properties. The alternative is upgrading AGP, which drags Kotlin and
# Compose Multiplatform with it. Not three weeks out.
android-compileSdk = "36"
android-minSdk = "26"
android-targetSdk = "36"
androidx-activityCompose = "1.9.3"
androidx-core = "1.15.0"
compose-multiplatform = "1.7.3"
kotlin = "2.1.0"
kotlinx-coroutines = "1.9.0"
kotlinx-datetime = "0.6.1"
kotlinx-serialization = "1.7.3"
ktor = "3.0.3"
# Native Android SDK, not purchases-kmp. purchases-kmp 3.x requires Kotlin
# 2.3.20+ and this project is pinned to 2.1.0 — see the note at the top of this
# file. Upgrading Kotlin cascades into Compose Multiplatform and AGP, which is a
# multi-day toolchain fight, not a billing integration. The iOS actual gets the
# native iOS SDK the same way when the iOS project exists.
revenuecat = "10.15.1"
sqldelight = "2.0.2"

[libraries]
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "androidx-activityCompose" }
androidx-core-ktx = { module = "androidx.core:core-ktx", version.ref = "androidx-core" }
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "kotlinx-coroutines" }
kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "kotlinx-coroutines" }
kotlinx-datetime = { module = "org.jetbrains.kotlinx:kotlinx-datetime", version.ref = "kotlinx-datetime" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinx-serialization" }
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }
ktor-client-core = { module = "io.ktor:ktor-client-core", version.ref = "ktor" }
ktor-client-okhttp = { module = "io.ktor:ktor-client-okhttp", version.ref = "ktor" }
ktor-client-darwin = { module = "io.ktor:ktor-client-darwin", version.ref = "ktor" }
ktor-client-content-negotiation = { module = "io.ktor:ktor-client-content-negotiation", version.ref = "ktor" }
ktor-serialization-json = { module = "io.ktor:ktor-serialization-kotlinx-json", version.ref = "ktor" }
revenuecat-purchases = { module = "com.revenuecat.purchases:purchases", version.ref = "revenuecat" }
sqldelight-android-driver = { module = "app.cash.sqldelight:android-driver", version.ref = "sqldelight" }
sqldelight-native-driver = { module = "app.cash.sqldelight:native-driver", version.ref = "sqldelight" }
sqldelight-coroutines = { module = "app.cash.sqldelight:coroutines-extensions", version.ref = "sqldelight" }

[plugins]
androidApplication = { id = "com.android.application", version.ref = "agp" }
composeMultiplatform = { id = "org.jetbrains.compose", version.ref = "compose-multiplatform" }
composeCompiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlinMultiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
kotlinSerialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
sqldelight = { id = "app.cash.sqldelight", version.ref = "sqldelight" }
